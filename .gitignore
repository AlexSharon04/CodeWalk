# Local agent / IDE state
.claude/
# .entire/ is intentionally tracked — it is the AI agent session log
# the course expects to see. Its own .entire/.gitignore excludes
# tmp/, metadata/, logs/, and settings.local.json.

# Node / build artifacts
node_modules/
out/
dist/
*.vsix

# OS / shell crash dumps
*.stackdump
Thumbs.db
.DS_Store

# Logs
*.log
